#include <algorithm>
#include <stdio.h>
#include <vector>
#include <cstring>
#include <queue>
#include <string>
#include <stack>

using namespace std;

int a, b;

int main() {
    scanf("%d%d", &a, &b);
    printf("%d %d", b - a, b);
    return 0;
}